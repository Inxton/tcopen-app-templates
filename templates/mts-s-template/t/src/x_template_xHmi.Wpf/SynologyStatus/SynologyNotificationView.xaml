<UserControl x:Class="x_template_xHmi.Wpf.SynologyStatus.SynologyNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:x_template_xHmi.Wpf.SynologyStatus"
             xmlns:synologystatus="clr-namespace:x_template_xHmi.Wpf.SynologyStatus" 
             xmlns:vortex="http://vortex.mts/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:properties="clr-namespace:x_template_xHmi.Wpf.Properties"
             mc:Ignorable="d"
             x:Name="View"
             d:DesignHeight="450" d:DesignWidth="800">

    <d:UserControl.DataContext>
        <synologystatus:SynologyStatusViewModel/>
    </d:UserControl.DataContext>


    <UserControl.Resources>
        <Style x:Key="PanelRow" TargetType="StackPanel">
            <Setter Property="MaxHeight" Value="100"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="iconContainer" TargetType="Border">
            <Setter Property="Visibility" Value="Visible"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderBrush" Value="{StaticResource Surface}" />
            <Setter Property="Opacity" Value="0.9" />
            <Setter Property="BorderThickness" Value="2.5"/>
            <Setter Property="CornerRadius" Value="3"/>
        </Style>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    
    <Grid Margin="5">
        <StackPanel Orientation="Horizontal" Style="{StaticResource PanelRow}" Visibility="Visible">

            <Border Style="{StaticResource iconContainer}" Margin="2" Background="{Binding ForegroundColor}">
                <materialDesign:PackIcon Kind="Database" Foreground="{StaticResource Surface}" Height="30" Width="30"/>
            </Border>

            <TextBlock Text="{x:Static properties:NotificationPanel.SynologyStatus}"
                       Foreground="{StaticResource Surface}" 
                       Margin="5" FontSize="20"
                       Visibility="{Binding IsExpanded, ElementName=View, Converter={StaticResource BooleanToVisibilityConverter}}"
                       VerticalAlignment="Center"/>

        </StackPanel>
    </Grid>
</UserControl>
