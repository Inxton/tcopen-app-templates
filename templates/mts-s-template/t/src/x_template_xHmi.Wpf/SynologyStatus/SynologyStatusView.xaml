<UserControl x:Class="x_template_xHmi.Wpf.SynologyStatus.SynologyStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:synologystatus="clr-namespace:x_template_xHmi.Wpf.SynologyStatus" xmlns:vortex="http://vortex.mts/xaml"
             d:DataContext="{d:DesignInstance Type=synologystatus:SynologyStatusViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="300">
    <UserControl.Resources>
        <materialDesign:PackIconExtension x:Key="SystemHealthIcon" Kind="HandOkay" />
        <materialDesign:PackIconExtension x:Key="WarningIcon" Kind="WarningOutline" />
        <materialDesign:PackIconExtension x:Key="StorageIcon" Kind="Storage" />
        <!--<converters:IconConverter x:Key="IconConverter" />-->
    </UserControl.Resources>

    <Grid Visibility="{Binding IsVisible ,Converter={vortex:BoolToVisibilityConverter}, ConverterParameter=true }">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center" >
            <materialDesign:PackIcon Kind="Storage"   Width="20" Height="20" Foreground="{Binding ForegroundColor}"/>
            <TextBlock Text="SYNOLOGY STATUS" FontSize="14" Foreground="{Binding ForegroundColor}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="{Binding BackgroundColor}">
            <materialDesign:PackIcon Width="50" Height="50"  Kind="{Binding StatusIcon}" Foreground="{Binding ForegroundColor}"/>
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Status}" Foreground="{Binding ForegroundColor}" FontSize="12" TextWrapping="Wrap" MaxWidth="100"/>
          </StackPanel>
    </Grid>
</UserControl>