<UserControl x:Class="x_template_xPlc.PscPanaTraceDataPacketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border VerticalAlignment="Top">
        <WrapPanel>
            <GroupBox Header="Length" Margin="5">
                <TextBlock Padding="5" Text="{Binding Length}" />
            </GroupBox>
            <GroupBox Header="Command" Margin="5">
                <TextBlock Padding="5" Text="{Binding Command}" />
            </GroupBox>
            <GroupBox Header="Workplace" Margin="5">
                <TextBlock Padding="5" Text="{Binding Workplace}" />
            </GroupBox>
            <GroupBox Header="Rfid" Margin="5">
                <TextBlock Padding="5" Text="{Binding Rfid}" />
            </GroupBox>
            <GroupBox Header="MasterNo" Margin="5">
                <TextBlock Padding="5" Text="{Binding MasterNo}" />
            </GroupBox>
            <GroupBox Header="Dps1" Margin="5">
                <TextBlock Padding="5" Text="{Binding Dps1}" />
            </GroupBox>
            <GroupBox Header="Dps2" Margin="5">
                <TextBlock Padding="5" Text="{Binding Dps2}" />
            </GroupBox>
            <GroupBox Header="Dps3" Margin="5">
                <TextBlock Padding="5" Text="{Binding Dps3}" />
            </GroupBox>
            <GroupBox Header="Dps4" Margin="5">
                <TextBlock Padding="5" Text="{Binding Dps4}" />
            </GroupBox>
            <GroupBox Header="Dps5/Ref" Margin="5">
                <TextBlock Padding="5" Text="{Binding Dps5}" />
            </GroupBox>
            <GroupBox Header="OperatorId" Margin="5">
                <TextBlock Padding="5" Text="{Binding OperatorId}" />
            </GroupBox>
            <GroupBox Header="Data" Margin="5">
                <TextBlock Padding="5" Text="{Binding Data}" />
            </GroupBox>
            <GroupBox Header="Result" Margin="5">
                <TextBlock Padding="5" Text="{Binding Result}" />
            </GroupBox>
            <GroupBox Header="Checksum" Margin="5">
                <TextBlock Padding="5" Text="{Binding Checksum}" />
            </GroupBox>
        </WrapPanel>
    </Border>
</UserControl>
