<UserControl x:Class="x_template_xPlc.TcoPscPanaTraceServiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:p="clr-namespace:x_template_xHmi.Wpf.Properties"
             xmlns:vortex="http://vortex.mts/xaml" xmlns:local="clr-namespace:x_template_xPlc" xmlns:materialdesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:UserControl.DataContext>
        <local:TcoPscPanaTraceViewModel />
    </d:UserControl.DataContext>
    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="HorizontalWraped">
            <WrapPanel Background="Transparent" Width="{Binding (FrameworkElement.ActualWidth) , RelativeSource={RelativeSource AncestorType=ScrollContentPresenter}}" MinWidth="{Binding ItemWidth , RelativeSource={RelativeSource Self}}" ItemHeight="{Binding (ListView.View).ItemHeight , RelativeSource={RelativeSource AncestorType=ListView}}" ItemWidth="{Binding (ListView.View).ItemWidth , RelativeSource={RelativeSource AncestorType=ListView}}" />
        </ItemsPanelTemplate>

    </UserControl.Resources>
    <vortex:TcoComponentView >
        <vortex:TcoComponentView.ComponentHeader>

            <WrapPanel VerticalAlignment="Center"   HorizontalAlignment="Left" DataContext="{Binding Component}" >
                <WrapPanel.Resources>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignBody1TextBlock}">
                        <Setter Property="VerticalAlignment" Value="Center"></Setter>
                    </Style>

                </WrapPanel.Resources>


                <WrapPanel    Visibility="{Binding Client.ClientSocket.Connected,Converter={vortex:BooleanToVisibilityConverter}}" Margin=" 5,0">
                    <materialdesign:PackIcon Kind="LanConnect"  Foreground="{StaticResource Notification}"  VerticalAlignment="Center"/>
                    <TextBlock   Text="Connected"  Foreground="{StaticResource Notification}"/>
                </WrapPanel>

                <WrapPanel     Visibility="{Binding Client.ClientSocket.Connected,Converter={vortex:BooleanToVisibilityConverter},ConverterParameter=true}" Margin=" 5,0">
                    <materialdesign:PackIcon Kind="LanDisconnect"  Foreground="{StaticResource Alert}"  VerticalAlignment="Center"/>
                    <TextBlock   Text="Disconnected"  Foreground="{StaticResource Alert}"/>
                </WrapPanel>
                <vortex:RenderableContentControl Margin="5" DataContext="{Binding _restoreTask }" PresentationType="Base" />

            </WrapPanel>


        </vortex:TcoComponentView.ComponentHeader>
        <vortex:TcoComponentView.ComponentDetails>

            <Grid  DataContext="{Binding Component}">

                <Grid.Resources>
                    <Style TargetType="GroupBox"  BasedOn="{StaticResource MaterialDesignCardGroupBox}">
                        <Setter Property="Margin" Value="5,0,0,5"></Setter>
                    </Style>
                    <Style TargetType="WrapPanel">
                        <Setter Property="MinWidth" Value="300"></Setter>

                    </Style>
                </Grid.Resources>

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>

                    <RowDefinition></RowDefinition>


                </Grid.RowDefinitions>

                <GroupBox  Padding="10" Header="{x:Static p:strings.Tasks}" >
                    <ItemsControl ItemsPanel="{StaticResource HorizontalWraped}" HorizontalAlignment="Left" ItemsSource="{Binding Tasks}">

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <vortex:RenderableContentControl MinWidth="150" DataContext="{Binding}" Margin="5"></vortex:RenderableContentControl>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <StackPanel Grid.Row="1" >
                    <GroupBox Header="Request">
                        <local:PscPanaTraceDataPacketView DataContext="{Binding Request}"/>
                    </GroupBox>
                    <GroupBox Header="Response">
                        <local:PscPanaTraceDataPacketView DataContext="{Binding Response}"/>
                    </GroupBox>
                </StackPanel>

                <WrapPanel Grid.Row="2">
                  

                    <GroupBox   Padding="10"  DataContext="{Binding _status}"  Header="{Binding AttributeName}">
                        <vortex:RenderableContentControl  DataContext="{Binding }" PresentationType="DisplaySlim"    />
                    </GroupBox>
                    <GroupBox Padding="10" Header="{Binding _config.AttributeName}">
                        <vortex:RenderableContentControl  DataContext="{Binding _config}" PresentationType="ControlSlim" />
                    </GroupBox>
                    <GroupBox Grid.Row="1"  Padding="10"  DataContext="{Binding _control}"  Header="{Binding AttributeName}">
                        <StackPanel>
                        <vortex:RenderableContentControl  DataContext="{Binding }" PresentationType="ControlSlim"    />
                        
                                <WrapPanel Orientation="Vertical" DataContext="{Binding PcbSerials}">
                                 
                                        <ItemsControl ItemsSource="{Binding }">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <GroupBox Header="{Binding Symbol}">
                                                        <vortex:RenderableContentControl Margin="10" DataContext="{Binding }"  PresentationType="ControlSlim" />
                                                    </GroupBox>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>


                        

                                </WrapPanel>
                     
                      
                        </StackPanel>
                    </GroupBox>
                    


                </WrapPanel>
            </Grid>
        </vortex:TcoComponentView.ComponentDetails>
    </vortex:TcoComponentView>
    <!--<Border VerticalAlignment="Top">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Row="0"
                Margin="5"
                Text="{Binding Component.AttributeName}" />
                <StackPanel Grid.Row="1" >
                    <GroupBox Header="Request">
                        <local:PscPanaTraceDataPacketView DataContext="{Binding Component.Request}"/>
                    </GroupBox>
                    <GroupBox Header="Response">
                        <local:PscPanaTraceDataPacketView DataContext="{Binding Component.Response}"/>
                    </GroupBox>
                </StackPanel>
            <DockPanel Grid.Row="2" Margin="10">
                <StackPanel Margin="10">
                    <vortex:RenderableContentControl DataContext="{Binding Component._manualResetCommand}" PresentationType="Manual" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._manualConfirmSerialNoCommand}" PresentationType="Manual" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._manualGenerateSerialNoCommand}" PresentationType="Manual" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._manualPcbWeddingCommand}" PresentationType="Manual" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._manualSendReportCommand}" PresentationType="Manual" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._manualGetStatusCommand}" PresentationType="Manual" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._manualInitializeCommand}" PresentationType="Manual" />
                </StackPanel>
                <StackPanel Margin="10">
                    <vortex:RenderableContentControl DataContext="{Binding Component._status}" PresentationType="Display" />
                    <vortex:RenderableContentControl DataContext="{Binding Component.IpAddress}" PresentationType="Display" />
                    <vortex:RenderableContentControl DataContext="{Binding Component.Port}" PresentationType="Display" />
                    <vortex:RenderableContentControl DataContext="{Binding Component.Workplace}" PresentationType="Display" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._rfid}" PresentationType="Control" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._partSerialNo}" PresentationType="Control" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._generatedSerialNo}" PresentationType="Display" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._pcbSerialNo}" PresentationType="Control" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._operatorId}" PresentationType="Control" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._result}" PresentationType="Control" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._errorMessage}" PresentationType="Control" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._tracabilityResponse}" PresentationType="Control" />
                    <vortex:RenderableContentControl DataContext="{Binding Component._dataFromTracability}" PresentationType="Control" />
                </StackPanel>
            </DockPanel>
        </Grid>

    </Border>-->
</UserControl>
